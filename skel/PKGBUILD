# Maintainer: Victor3D <webmaster@victor3d.com.br>
pkgname=skel-victor3d
pkgver=1
pkgrel=1
epoch=
pkgdesc="Home Skel"
arch=('x86_64')
url="http://wiki.victor3d.com.br"
license=('GPL')
groups=()
depends=("openbox")
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("https://github.com/victor3dptz/arch-install/raw/master/skel/skel.tar.gz")
noextract=()
md5sums=("e64f3bda16413f940c97a8f7bf8a4f78")
validpgpkeys=()

prepare() {
cd
}

build() {
cd
}

check() {
cd
}

package() {
#	mkdir -p "$pkgdir/etc/skel"
	cp -Rp "etc/" "$pkgdir/"
}
